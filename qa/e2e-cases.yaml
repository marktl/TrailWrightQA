baseUrl: "{{env.local}}"
project: chromium
viewport: { width: 1366, height: 900, headless: true }

scenarios:
  - name: example-scenario
    usingAccount: user
    steps:
      - navigate: "{{baseUrl}}/"
      - expectText: { contains: "Welcome" }

  # Add your test scenarios here
  # Use data-testid attributes in your UI for reliable selectors
  # Example:
  # - name: user-can-login
  #   usingAccount: user
  #   steps:
  #     - navigate: "{{baseUrl}}/login"
  #     - fill: { selector: "#email", value: "{{accounts.user.email}}" }
  #     - fill: { selector: "#password", value: "{{accounts.user.password}}" }
  #     - click: { selector: "button[data-testid='login-submit']" }
  #     - expectText: { contains: "Dashboard" }
  #     - expectResponse: { url: "/api/user", status: 200 }
